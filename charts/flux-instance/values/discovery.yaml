# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT
#
# Discovery-only values. The chart renders only the FluxInstance CR — every
# image it implies (the fluxcd distribution controllers) is declared as
# images.extra in manifest.yaml instead.
{}
